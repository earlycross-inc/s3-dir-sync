{
  "name": "s3-dir-sync",
  "version": "0.1.0",
  "description": "command line utility to synchronize from a local directory to s3 bucket",
  "files": [
    "bin"
  ],
  "bin": {
    "s3-dir-sync": ".bin/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/earlycross-inc/s3-dir-sync.git"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/earlycross-inc/s3-dir-sync/issues"
  },
  "homepage": "https://github.com/earlycross-inc/s3-dir-sync#readme",
  "scripts": {
    "lint:eslint": "eslint --fix --ext .tsx --ext .ts src/",
    "format": "prettier --write src/**/*.{js,ts}",
    "lint": "tsc --noEmit && yarn lint:eslint && yarn format",
    "start": "ts-node src/Upload.ts --config=test",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@types/argv": "^0.0.4",
    "@types/aws-sdk": "^2.7.0",
    "@types/fs-extra": "^9.0.11",
    "@types/node": "^15.3.1",
    "argv": "^0.0.2",
    "aws-sdk": "^2.910.0",
    "fs-extra": "^10.0.0"
  },
  "devDependencies": {
    "eslint": "^7.26.0",
    "eslint-config-earlycross-typescript": "^0.2.3",
    "eslint-config-prettier": "^8.3.0",
    "prettier": "^2.3.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
